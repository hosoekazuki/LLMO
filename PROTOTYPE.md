# アプリ開発について

### 構成の方針

- **画面とロジックを分ける。** `app.py` は表示だけを行い、評価の処理は `llmo/` にある。
  フロントを別のものに差し替えるとき、ロジック側に手を入れずに済むようにするため。
- **画面が呼ぶのは `pipeline.py` だけにする。** 個々のモジュールを画面から直接呼ぶと、
  処理の順序が画面側に散らばってしまう。
- **実験条件は `config.py` に集約する。** 取得件数やチャンク長をコード中に直接書くと、
  「どの条件で測った結果なのか」が後から分からなくなる。
- **生成物は `data/` にまとめ、リポジトリに入れない。** 取得したウェブページの本文や
  API キーを含むものが混ざらないようにするため。
- **`llmo/` の中は処理の段階ごとに分ける。** ファイルを平らに並べると、
  どれが「記事を作る側」でどれが「評価する側」なのかが名前からしか分からない。
  段階で切っておくと、変更の影響がその段階の中で閉じているかを判断しやすい。
- **パスは実行ディレクトリ基準で書く。** モジュールの位置（`__file__`）を起点にすると、
  ファイルを別のディレクトリへ移した時点でデータの読み書き先が変わってしまう。

---

## 背景

急成長中の SaaS がコンテンツを作成する中で、近年の LLM ブームに従って LLMO 最適化することによって、LLM による検索に選択・引用されることが重要だと考え始めた。そこで、LLMO 最適化を自動で行うシステムを作成することにする。

---

## そもそもこの仮説は正しいのか？

そもそも「LLMO 最適化するべき」という考え方は正しいのかというのを確認しておく必要がある。
確認するべき事項は以下のとおりである。**定量的に考えること**を重要視する。

### 1. LLM の利用率というのは、大方の予想通り上昇傾向にあるのか？

これはおそらく正しい。いろいろなソースはあると思うが、以下が参考文献である。

- https://www.moba-ken.jp/project/lifestyle/20260406.html

### 2. LLM による検索というのは増えており、その重要度は高まっているのだろうか？

正しいと考えられる。

- LLM の検索結果のみで満足し、それ以上クリックしないというのは統計的にも増えていると考えられる。
- しかし、LLM 検索が一般的なウェブ検索よりも重要とは言い切れない。あくまで重要度が増しているというレベルである。

参考文献:

- https://www.moba-ken.jp/project/lifestyle/20260205.html
- https://sparktoro.com/blog/in-2026-less-than-one-third-of-google-searches-still-send-a-click/

### 結論

上記を踏まえて、LLMO 最適化というのは行う意味があると考えられる。

---

## 業務課題にまで再定義

上記の内容だけでは、「実際に重要になっている、だから意識しよう」という程度のものになってしまっている。
じゃあ、それをシステムにするうえで解決できる業務課題とは何なのか。それが完成したらどのような利点が生まれ、そしてどのような効果が出ると考えられるのかを考える。

### 現状の言語化

今は、「LLMO 大事だな」と考えて、それを個人が意識してマーケティング部が行っているのみの状態と考えられる。
つまり、以下の課題があげられる。

- 個人のノウハウに依存していること
- それが実際に効果が出ているのかわからないこと
- 再現性が低いこと

### 各課題を解決できるのか

ではそれを解決することができるのかというのを示す必要がある。

| 課題 | 解決の見込み |
| --- | --- |
| 個人のノウハウへの依存 | LLM のプロンプトは共通になり、また人間の評価を踏まえ、そのプロンプトを改善しつつ LLM の精度を向上させていくことができるので解消されるだろう。 |
| 効果が出ているのかわからない | 記事を作成するシステムだけでなく、作成した記事を実際に公開したとしたらどのような効果が出るだろうかというのを検証する部分を実装する予定なので、それも解決できるだろう。 |
| 再現性の低さ | LLM なので毎回全く同じというわけにもいかないとは思うが、別の人間がやるよりは効果があるのではないかと考えられる。 |

### 数字で示すと（想定される生産性向上効果）

> **注意:** 以下の数値はすべて仮置きの前提であり、実際の値は未確定である。

**前提（仮置き）:**

| 項目 | 仮置きの値 |
| --- | --- |
| 記事作成本数 | 月 10 本 |
| 1 記事あたりの作成・LLMO 最適化にかかる時間 | 5 時間 → 1 時間 |
| 時間単価 | 1,500 円 |

**この前提から導かれる効果:**

- 月 40 時間、年間 480 時間の工数削減が見込まれる。
- 年間約 72 万円相当の工数削減効果となる。

※実際の効果は導入後の作業時間を計測して検証する。

---

## LLMO とは？その手法として有効と考えられるものには何があるのか？

### LLMO とは

グーグル検索ではなく、生成 AI による検索にヒットしやすく、引用されやすいことを意識したコンテンツの作成方法である。

### 手法として取り上げられているもの

いくつか論文も出版されている。

- The Influence of Retrieval, Fan-out, and Google SERPs on ChatGPT Citations
- Staying Seen In AI Search: How Citations & Mentions Impact Brand Visibility
- Aggarwal et al., "GEO: Generative Engine Optimization", KDD 2024（arXiv:2311.09735）
- Diagnosing and Repairing Citation Failures in Generative Engine Optimization

**有効と考えられるもの:**

- クエリと文書の関連性を高めること
- コンテキスト内での位置
- 抽出可能な根拠が明確に含まれていること

**効果が検証されていないもの:**

- 文書の構造化がなされていること
- llm.txt の作成

---

## LLM のウェブ検索方法から考える、効果があると思われる方法について

LLM の回答生成の流れは以下のとおりである。

1. ユーザーの自然言語による入力から内容を汲み、クエリを複数作成する。
2. それをもとにウェブ検索を実行する。
3. 得られた文章の内容を要約し、コンテキストとして取り込む。
4. それらをもとに適宜内容を引用しながら、回答を生成する。

つまり、LLM が回答を作成する流れの中で、人間が作成した記事が意識するべきポイントは以下である。

- LLM がウェブ検索をした時に、結果の中に含まれていること。
- 検索した結果に含まれていたうえで、出力の中で多くの部分が引用されていて、回答の上部に表示されていること。

これらを達成できるような記事を作成できれば、このシステムの出力は利用する意味があると考えられる。

---

## 今回作成するアプリの概要について

### 入力と出力

入力には 2 つのパターンがあり、それぞれ処理と出力、そして評価の仕方が異なる。
以下では、システムが扱う公開前の記事をまとめて**未公開記事**と呼ぶ。

#### パターン A: 記事のトピックのみを入力する場合

- **入力:** 作成したい記事のトピック。
- **処理:**
  1. そのトピックから、LLM が**タイトル案を複数提示**する。
  2. ユーザーがその中から 1 つを選ぶ。
  3. 選ばれたタイトルで、LLM が LLMO 最適化された記事を新規に作成する。
- **出力:** 作成した未公開記事と、その記事を検索対象に含めた状態での評価結果。

このパターンでは**比較は行わない**。書き直す前にあたるものが存在しないためである。
評価は、作成した記事を含めて検索を実行したときに、それが検索にヒットし、回答に引用されるかを見るのみとなる。

#### パターン B: すでに人間が作成した記事を入力する場合

- **入力:** 人間がすでに作成した記事（未公開のもの）。
- **処理:**
  1. その記事の内容から、LLM が**書き直し後のタイトル案を複数提示**する。
  2. ユーザーがその中から 1 つを選ぶ。
  3. 選ばれたタイトルで、LLM がその記事を LLMO 最適化された記事に書き直す。
- **出力:** 書き直した未公開記事と、**書き直す前と後の比較**による評価結果。

このパターンでは、書き直す前の記事を含めた検索と、書き直した後の記事を含めた検索をそれぞれ実行し、その結果を比較する。
比較したいのはあくまで**書き直す前と後**であり、記事を検索対象に含めるか否かではない。

### 設計方針

- 書き直しの基準というのは、プロンプトで事前に指示を与えておく。
- これをルールベースにする方法も考えたが、記事の作成はルールベースで行うよりも LLM に任せる方が有効だと判断した。

#### タイトルをユーザーに選ばせる理由

記事の作成に入る前に、タイトル案の提示とその選択という段階を挟む。

- タイトルは、**検索クエリと最も直接ぶつかる要素**である。
  同じトピックでも切り口が変われば、どの質問に引き当てられるかが変わる。
- 本文の書き方は評価基準から逆算して機械的に指示できるが、
  **どの切り口で書くかは事業の判断**であり、システムが自動で決めてしまうべきではない。
- タイトル案の作成は数秒で終わる軽い処理であるため、
  気に入る案が出るまで何度でも出し直せる。
  一方、記事の作成と評価には数分かかる。
  重い処理に進む前に方向を確定させることで、やり直しの手間を抑えられる。

提示する案の数は設定値として持ち、変更できるようにする。
少なすぎると似た案ばかりで選択肢が尽き、多すぎると読み比べる負担が勝つためである。

なお、タイトル案の作成にも**評価に使うクエリと同じものを渡す**。
タイトル案・記事の作成・評価で別のクエリを使うと、
「読者がその質問をする」という前提が段階ごとにずれてしまうためである。

### 検証の必要性

未公開記事の作成まで終わったら、次に実際にその記事が意味を持つのか、つまり実際に公開したのちにそれが LLM によって引用されるのかというのを検証・評価する必要がある。

その手法として、評価のたびに LLM を用いてウェブ検索を行い、それによって実際に出力を生成してみて、そのうちに含まれているかというのを定量的に評価するというステップを踏む。

### 評価方法

具体的な評価方法は以下である。

1. まず、LLM がユーザーの入力から有効なクエリを作成する。
2. そのクエリを用いて、Tavily Search などのウェブ検索用の API を用いて検索を実行し、
   取得したウェブページから**ベースコーパス**を作成する。これは以降固定して使い回す。
3. ベースコーパスに未公開記事を差し込んだうえでベクトル検索を実行し、
   その結果をもとに LLM の回答を生成して、定量的に評価する。
   評価する条件の数は入力のパターンによって異なる。

   | パターン | 評価する条件 | 見るもの |
   | --- | --- | --- |
   | A（トピックのみ） | ベースコーパス ＋ 作成した記事（1 条件） | その記事が単体でどの水準に達したか |
   | B（既存記事） | ベースコーパス ＋ 書き直し前の記事／書き直し後の記事（2 条件） | 書き直しによってどう変化したか |

4. パターン B では、どこがどのように変化して、どのような効果が生まれたのかを出力する。

なお、いずれのパターンでも**未公開記事は必ず 1 本だけ差し込む**。
差し込む本数を条件によって変えると、検索の枠を奪い合う数が変わってしまい、
記事そのものの効果と混ざるためである。

### 疑似ウェブによる評価環境

上記を実現するために、**一度ウェブから取得した内容を固定してコーパス化し、そこに対するベクトル検索によって疑似的にウェブ検索を再現する**という構成をとる。

**なぜ LLM に自律的に検索させないのか**

- システムが扱うのは未公開記事であり、公開されていない以上、LLM に自律的にウェブ検索をさせても検索結果に含めることができない。
- また、自律的に検索させると実行のたびに検索結果が変動してしまい、パターン B の書き直し前後の比較において、記事以外の要因で結果が変わってしまう。
- そこで、Tavily を挟んで取得した結果から**疑似的なウェブ（コーパス）を作成して固定**し、そこに未公開記事を差し込んでベクトル検索を実行することで、未公開のまま、かつ条件を揃えた評価ができるようにする。

**処理の段階**

タイトルの選択でユーザーの操作を挟むため、処理は 2 段階に分かれる。

| 段階 | 行うこと | 目安 |
| --- | --- | --- |
| 1 | クエリ生成 → タイトル案の提示 | 数秒 |
| 2 | 疑似ウェブの構築 → 記事の作成 → 検索 → 回答生成 → 評価 | 数分 |

1 段階目で作ったクエリは、2 段階目でもそのまま使い回す。

**共通の前処理**

パターン A・B のいずれの場合も、評価の土台となる疑似ウェブを作成する。

1. **クエリ生成** — LLM がユーザーの入力から複数のクエリを作成する。
2. **本文の抽出** — 生成したクエリで Tavily を実行し、取得したウェブページから広告・ナビゲーション・サイドバーなどの本文でない要素を切り捨て、**記事本文のテキストのみ**を抽出する。
3. **チャンク分割と保存** — 抽出した本文を一定の長さのチャンクに分割し、埋め込みベクトルとともに保存する。これを**ベースコーパス**とする。以降、ベースコーパスは固定して使い回す。埋め込みは手元のモデルで行うため、API の枠を消費しない。

**チャンク分割にあたっての注意点**

本文をそのままチャンクに分割してしまうと、**どのチャンクが同じ記事に属していたのかがわからなくなる**。
今回の評価基準は言及率（Mention Rate）や引用率（Citation Rate）のように**記事（文書）単位**で数えるものであるため、チャンクが断片のまま扱われると評価が成立しない。

そこで、各チャンクには以下のメタデータを持たせ、元の記事に必ず復元できるようにしておく。

| 保持する情報 | 用途 |
| --- | --- |
| 記事 ID | どの記事に属するチャンクかを識別する。検索結果を記事単位に集約するために使う。 |
| 記事内での並び順 | 同じ記事のチャンクを元の順序に戻す。「コンテキスト内での位置」を評価する際にも使う。 |
| URL・タイトル | 回答生成時の引用元の提示、および記事の同定に使う。 |
| 区分（取得記事／未公開記事） | 疑似ウェブ由来か、システムが扱う未公開記事かを区別する。 |

未公開記事についても、取得したウェブページと**同じ手順で本文抽出とチャンク分割を行う**。
前処理が条件によって変わってしまうと、それ自体が検索順位の差を生んでしまい、比較の妥当性が損なわれるためである。

**評価実行時の流れ（共通）**

コーパスができたあとの回答生成は、いずれのパターンでも次の流れをとる。

1. クエリの埋め込みベクトルと各チャンクのベクトルを比較し、クエリに答えている可能性が高いチャンクを上位から取得する。
2. 取得したチャンクを記事 ID で集約し、どの記事がどの順位で引き当てられたかを記録する。集約の際、記事の代表スコアには最も高かったチャンクの値を用いる。平均を取ると、長い記事ほど本題と関係ない部分に薄められて不利になるためである。
3. 上位に入った記事の**本文**をコンテキストとして LLM に渡し、**モデルの内部知識と統合**させたうえで、引用元を示しながら回答テキストを生成する。

ここで LLM に渡すのは、検索で当たったチャンクではなく**記事の本文**である。
実際の生成 AI は、検索エンジンが返したページの本文をそのままコンテキストに入れて回答を書く。
チャンクだけを渡すと、記事のどこを読ませるかをこちら側が先回りして決めてしまうことになり、
引用先の選択が実態から離れる。
**ベクトル検索は「どの記事を読ませるか」を決めるところまでで役目を終え、そこから先は本文に戻る。**

ただし本文をそのまま渡すと、数万字の未公開記事と数千字の他社記事が並んだときに、
自社記事だけがコンテキストを占有して有利になる。
これは記事の質ではなく分量による差であるため、**全記事に共通の文字数上限をかけて切り揃える**。
上限は実験条件として `config.py` に持ち、評価結果とともに記録する。
なお上限で切り揃えても、これより短い記事は短いままなので、分量の差が完全に消えるわけではない。

ここから先は、コーパスに何を差し込むかと、比較を行うかどうかが入力のパターンによって変わる。

**パターン A の流れ（比較なし・単体評価）**

1. **コーパスの用意** — ベースコーパスに、作成した未公開記事を追加する。条件は 1 つのみである。
2. **検索の再現** — そのコーパスに対して、生成した各クエリでベクトル検索を実行し、上位の文書を取り出す。
3. **回答生成** — 取り出した文書をコンテキストとして LLM に渡し、回答を生成する。
4. **評価** — 作成した記事が検索結果の上位に入ったか、また生成された回答に引用されたかを[評価に使用する基準](#評価に使用する基準)で定量化する。
   ここでは前後の比較を行わず、その記事が単体でどの程度の水準に達しているかを示す。

**パターン B の流れ（書き直し前後の比較）**

1. **2 条件のコーパスを用意する** — ベースコーパスは共通のまま、差し込む記事だけを入れ替える。
   - **対照群（before）:** ベースコーパス ＋ 人間が作成した書き直し前の記事
   - **実験群（after）:** ベースコーパス ＋ LLM が書き直した後の記事
2. **検索の再現** — それぞれのコーパスに対して、同じクエリでベクトル検索を実行し、上位の文書を取り出す。
3. **回答生成** — 取り出した文書をコンテキストとして LLM に渡し、それぞれ回答を生成する。
4. **比較・評価** — 2 つの回答を[評価に使用する基準](#評価に使用する基準)で定量化し、どこがどのように変化して、どのような効果が生まれたのかを出力する。

このとき、2 条件の差分は**差し込んだ記事が書き直し前か後か**だけである。ベースコーパスもクエリも共通であるため、観測された差は書き直しによる効果とみなすことができる。

**この構成が満たしている条件**

- 疑似ウェブは一度取得したら固定するので、前後の比較で検索結果が変わらない（「注意点」の 3 つ目を満たす）。
- 未公開記事を、公開することなく検索対象に含められる。これはパターン A の新規作成記事、パターン B の書き直し前後の記事のいずれにも当てはまる。
- ベクトル検索の順位という形で、記事が「検索結果に含まれるか」自体も観測できる。

---

## 評価に使用する基準

指標は **7 つ**である。記事が「検索で拾われ、回答に使われ、推される」という過程を
たどる順に並べてあり、どの段階で落ちたのかが分かるようにしてある。

- **1〜2** — 回答生成の**前**。ベクトル検索で何が起きたかを見る。
- **3〜7** — 生成された**回答**を見る。

指標の算出のされ方は、入力のパターンによって異なる。

- **パターン A（トピックのみ）:** 未公開記事を差し込んだ 1 条件のみで算出する。
  比較する相手が存在しないため、各指標は「その記事が単体でどの水準に達したか」を示す。
- **パターン B（既存記事の書き直し）:** 書き直し前（before）と書き直し後（after）の
  2 条件で算出し、その差を見る。

判定の方法は 2 種類ある。

- **機械的判定** — 文字列や出典番号の照合だけで出せるもの。指標 1〜5。
- **LLM 判定** — 文面を読んで判断する必要があるもの。指標 6・7。
  記事を書いたモデルとは別のモデルに判定させ、自分の文章を甘く採点する偏りを避ける。

### 検索の段階（回答生成前）

#### 1. 検索ヒット率（Retrieval Rate）

- **定義:** 自社記事がベクトル検索の上位（取得件数 K 件）に入ったクエリの割合。
- **分子:** 上位に入ったクエリ数 ÷ **分母:** 全クエリ数
- **判定:** 機械的判定（検索順位）。
- **補足:** 最初の関門。ここで拾われなければ回答の材料にすらならないため、引用と分けて見る。

#### 2. 検索順位（Average Search Rank）

- **定義:** 検索で拾われたときの、自社記事の平均順位（1 始まり）。
- **分母:** 拾われたクエリのみ。圏外のクエリは平均に含めない。
- **判定:** 機械的判定。
- **補足:** 1 が「上位に入れたか」を見るのに対し、こちらは「何位で入れたか」を見る。
  1 が高いのに 2 が低い（順位が下のほう）場合、枠の末尾で辛うじて拾われている状態であり、
  コーパスが変われば圏外に落ちうる。

### 回答の段階

#### 3. 言及率（Mention Rate）

- **定義:** 回答本文に自社名・製品名が 1 回以上登場したクエリの割合。
- **分子:** 自社名が登場したクエリ数 ÷ **分母:** 全クエリ数
- **判定:** 機械的判定（文字列照合）。照合に使う表記のゆれは自社情報として外部に持つ。
- **補足:** 引用とは独立して測る。出典番号を付けられなくても本文で名前を出されることはあり、
  逆に引用されても社名に触れられないこともあるため。
  自社記事が検索で拾われなかった場合でも、他社記事の中で自社名に触れられていれば値が出る。
  7 指標のうち、記事が圏外でも数字が出る唯一の指標である。

#### 4. 引用率（Citation Rate）

- **定義:** 自社記事が出典として引用されたクエリの割合。
  ここでいう「引用された」とは、回答本文に自社記事の出典番号（`[n]`）が
  1 回以上現れたことを指す。
- **分子:** 引用されたクエリ数 ÷ **分母:** 全クエリ数
- **判定:** 機械的判定（出典番号の照合）。
- **補足:** 3 が「名前が出たか」を見るのに対し、こちらは「自社記事が根拠として使われたか」を見る。

#### 5. 引用シェア（Citation Share）

- **定義:** 回答中の全引用数に占める、自社記事の引用数の割合。
- **分子:** 自社記事が引用された回数 ÷ **分母:** 回答中の引用の総数
- **判定:** 機械的判定（出典番号の数え上げ）。
- **補足:** 4 が「引用されたか否か」の 0/1 なのに対し、こちらは量を見る。
  回答の主役だったのか、他社の補足として一度触れられただけなのかを区別する。
  4 はクエリ数を分母とする離散値のため取りうる値が粗く、
  書き直しによる小さな変化はこちらのほうが検出しやすい。

#### 6. 最推奨率（Top Recommendation Rate）

- **定義:** 複数社を比較・紹介する回答のうち、自社が最も推奨されたものの割合。
- **分子:** 自社が最推奨だった回答数 ÷ **分母:** **比較形式と判定された回答数**
- **判定:** LLM 判定。「複数の選択肢を比較する回答か」と「最も推奨されているのはどれか」を
  LLM に判定させ、返ってきた名前が自社かどうかは表記ゆれを含めて機械的に照合する。
- **補足:** 引用されることと、選ばれることは別であるため分けて測る。
  分母を比較形式の回答に限るのは、そもそも 1 位を決める場面がない回答を
  分母に入れると、勝ちようがないものを負けとして数えてしまうため。
  比較形式の回答が 1 つも無い場合は算出しない。

#### 7. 根拠寄与率（Answer Grounding）

- **定義:** 回答の主張のうち、自社記事の記述が根拠になっている主張の割合。
- **分子:** 自社記事が支えた主張の数 ÷ **分母:** 回答から抽出された主張の総数
- **判定:** LLM 判定。回答を主要な主張に分解し、各主張が対象記事の記述で
  支えられているかを判定させる。他の資料にも同じ内容が書かれていそうだという理由で
  支持とはしない。
- **算出する条件:** 未公開記事が回答の出典に含まれた場合のみ。
  検索で拾われず出典に入らなかった場合は、支えようがないため算出しない。
  パターン B では書き直し前・後の両方で算出し、その差を見る。
- **補足:** 支えた主張とその根拠を記録に残す。
  1〜6 が「どうなったか」しか返さないのに対し、7 だけが**記事のどの記述が効いたか**を返す。
  裏返せば支えられなかった主張が記事の穴であり、**改善の手がかりになる唯一の指標**である。

### 指標どうしの関係

個々の値そのものより、**どこで落ちたか**を切り分けるために組み合わせて読む。
たとえば引用率が 0% だったとき、そもそも検索に拾われなかったのか、
拾われたが引用されなかったのかが分からないと、直すべき場所が決まらない。
前者なら記事とクエリの適合性、後者なら記事の中身という具合に、対処が正反対になる。

| 観測される組み合わせ | 読み取れること |
| --- | --- |
| 1 が低い | クエリと記事の適合性の問題。中身以前に見つかっていない。 |
| 1 は高いが 4 が低い | 見つかってはいる。中身が使うに値しないと判断されている。 |
| 4 は高いが 5 が低い | 引用はされるが脇役。他社の補足として使われている。 |
| 4 は高いが 6 が低い | 情報源としては使われるが、選ばれてはいない。 |
| 4 は高いが 7 が低い | 引用はされたが、回答の主張をほとんど支えていない。 |

---


## 注意点

- Tavily の検索結果は、厳密に生成 AI の検索結果と一致するものではないので代理指標である。
- 実運用では、公開後の実測も続ける必要がある。
- ウェブ検索の結果は、記事修正前後で同様のものを用いる。
- 疑似ウェブに対するベクトル検索は、実際の検索エンジンのランキングと同じではない。埋め込みモデルとチャンク分割の仕方によって順位が変わるため、これも代理指標である。

---

## 使用する技術について

### バックエンド: Python（Web フレームワークは未導入）

**Python を選んだ理由**

- 検索の段階でベクトル検索を用いる。これを実装するうえで、Python にはライブラリが複数あり、実装が容易になると考えた。
- Node.js でも実装自体は可能だが、ライブラリの存在によって Python を用いるようにした。

**Web フレームワークを現時点で導入していない理由**

評価のロジックは 1 つのモジュールに集約してあり、それを呼び出すのは Streamlit の画面のみである。
呼び出し元が 1 つしかない状態で API 層を挟んでも、経由するものが増えるだけで得るものがない。
また、評価は数分かかる同期的な処理であり、画面側で進捗を出すだけで足りている。

**導入を検討する条件**

以下のいずれかが発生した時点で、FastAPI の導入を検討する。

- Streamlit 以外のフロントエンド（Next.js など）から利用する必要が出たとき。
- 社内の別システムやバッチ処理から評価を実行する必要が出たとき。
- 評価をジョブとして投入し、結果を後から取得する形にする必要が出たとき。

**候補の比較（導入する場合）**

| 候補 | 判断 | 理由 |
| --- | --- | --- |
| **FastAPI** | **第一候補** | ドキュメントの自動生成と非同期の実装が容易な点。あとは自分が学習した経験があるからというのもある。 |
| Flask | 不採用 | あえて Flask にする理由がない。別にそうしてもいいけど、FastAPI の方が上記の内容から便利。 |
| Django | 不採用 | これはオーバースペックだから不要。 |

### フロントエンド: Streamlit

- Python をバックエンドで用いていて、一貫して Python で扱えるから。
- UI のコードの量が少ないので、時間をロジック部分に回せる。
- さらに、ロジックと明確に分離させることによって、後々フロントを拡張したいとなったらこれを変更する。又は捨てて Node.js とかを導入することによって変更が可能になる。

### LLM API: Gemini

- 無料枠が充実していること。
- 記事・回答の生成と、その評価とで**別のモデル**を用いることでバイアスがかかることを防ぐ。
  同じモデルに自分の書いた文章を評価させると、甘く採点する偏りが入るためである。

**現時点での構成**

| 用途 | モデル | 実行場所 |
| --- | --- | --- |
| 記事生成・クエリ生成・回答生成 | 生成用のモデル | Gemini API |
| 検索クエリの読者課題への言い換え | 変換用のモデル | Gemini API |
| 最推奨率（Top Recommendation Rate）・根拠寄与率（Answer Grounding）の判定 | 判定用のモデル（生成用とは別のもの） | Gemini API |
| チャンクとクエリの埋め込み | `cl-nagoya/ruri-v3-30m` | **手元（CPU）** |

### 埋め込みを手元で動かす理由

疑似ウェブを作り直すたびにコーパス全体（数百チャンク）を埋め込むため、
API では無料枠の上限に当たって評価が完走しない。
手元で動かせば回数の制限が無くなり、同じ入力に対して常に同じベクトルが出る。
API のモデルは更新されるとベクトルが変わるため、再現性の面でも都合がよい。

`cl-nagoya/ruri-v3-30m` は日本語に特化した約 37M パラメータのモデルで、256 次元。
CPU で動き、メモリ 4GB の環境にも収まる。

**初回の実行時に、Hugging Face からモデル（約 130MB）のダウンロードが発生する。**
2 回目以降は `~/.cache/huggingface` から読むので通信は起きない。
導入には PyTorch が必要で、CPU 版を明示して入れる
（既定のまま入れると CUDA 込みになり、GPU を使わないのに数 GB を消費する）。

```
pip install torch --index-url https://download.pytorch.org/whl/cpu
pip install sentence-transformers
```

ruri v3 は接頭辞を前提に学習されている。
文書には `検索文書: `、クエリには `検索クエリ: ` を付けて変換する。
付けないと精度が落ちる。接頭辞は変換の直前にだけ付け、保存する本文には含めない。
本文は画面表示と引用の判定に使うためである。
なお ruri は版によって接頭辞が異なる（初代は `文章: ` `クエリ: `）ので、
モデルを差し替えるときはモデルカードを確認する必要がある。

**残っている課題**

生成と判定を別のモデルに分けてはいるが、いずれも Gemini である。
同一プロバイダの同系列モデルであるため、バイアスの回避としては不完全である。
別プロバイダ（groq など）を判定側に充てることを検討したが、現時点では導入していない。

また、無料枠はモデルによって条件が大きく異なる。
日次のリクエスト上限が厳しいモデルでは評価が 1 回も完走しないため、
使用するモデルは上限を確認したうえで選定する必要がある。

### 検索 API: Tavily

- 無料枠が充実していること。
- LLM 向けに成型された本文が返ってくること。

### DB: SQLite

- SQLite で十分。評価結果を保存するのみだから、そんなに重いものを使わなくていい。
